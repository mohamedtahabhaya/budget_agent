// Configuration for the Budget Agent Mobile client
import { Platform } from 'react-native';

// We detected your computer's local IP as 192.168.1.12 from the Metro logs.
// This allows your physical Android phone running Expo Go on the same Wi-Fi to connect.
const DEV_API_URL = 'http://192.168.11.118:8000';

export const API_URL = DEV_API_URL;
export const DEFAULT_WORKSPACE_ID = 'workspace_coloc_taha_mohamed';
export const DEFAULT_USER_ID = 'user_mohamed';
